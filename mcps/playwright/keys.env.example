# playwright needs NO API key. Nothing to set up - install it and it works.
#
# It drives a real browser via npx @playwright/mcp@latest and uses Playwright's
# default browser profile, so it is fully portable across machines.
